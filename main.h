#ifndef MAIN_H
#define MAIN_H
#include <stdlib.h>
#include <stdio.h>

int _puts(char *str);
int _printf(const char *format, ...);
int _putchar(char c);
int print_decimal(int value);
int digit(int pin);

#endif
